-- Created by RamenDominoes (Feel free to credit or not I don't really care)
--Not bad for my first event created... I think

function onEvent(n,v1,v2)
	if n == 'Change Character Unwell' then
		triggerEvent("Change Character")
	end
end